# Private working notes — never published to the site/repo
programming-notes/

# OS / editor junk
.DS_Store
Thumbs.db
desktop.ini
*.log
.vscode/
.idea/
*.swp
*~

# Stray tooling installs (image/build tools agents may npm-install) — never published
node_modules/
package.json
package-lock.json

# QA / review scaffolding — never published (GitHub Pages serves the repo root).
# Root-anchored (leading /) so these can NEVER match real assets under assets/
# (the only real JS is assets/js/main.js, which is unaffected).
/_qa/
/_shots/
/__*.html
/_*.html
/_*.mjs
/_*.js
/_verify_*.png
/marquee_*.png
/marquee_*.html
